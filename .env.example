# Password required on /unlock to grant access to the dashboard.
SITE_ACCESS_PASSWORD=pass

# Optional: customize the salt used to derive the auth token.
# If omitted, the app falls back to a static default.
SITE_ACCESS_SALT=salt
